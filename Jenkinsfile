pipeline {
    agent any
    triggers{
    pollSCM('1****')
    }
    stages {
        stage('Build') {
            steps {
                echo'Building completed'
              }
            
        }
    }
}
